# App Store metadata

This directory is the repository source of truth for CloudBridge 1.0 (18) metadata. Localized fields are frozen from the approved ASO plan and must pass validation before upload.

