# Screenshot QA

Source set inspected: `/Users/user/Desktop/CloudBridge-shooh/AppStore成品`

All four source PNGs are 2880 × 1800 RGB.

## Release decision

The original first screenshot displayed a concrete server IP address and its headline included “terminal”. It has been remediated non-destructively as `01-browse-your-servers.png`, using the non-sensitive endpoint `demo.example.com` and the approved headline `Browse Your Servers`.

All four remediated screenshots are stored in this directory at 2880 × 1800 RGB. The English set may be inherited by every App Store localization.

- `01-browse-your-servers.png`: headline changed to `Browse Your Servers`; server IP replaced with `demo.example.com`.
- `02-preview-before-you-download.png`: background server IP replaced with `demo.example.com`.
- `03-every-download-in-sight.png`: all displayed server IPs replaced with `demo.example.com`.
- `04-private-by-design.png`: obsolete local-settings/Keychain claim replaced with the Build 18 Keychain statement.
