# Review Notes

CloudBridge is a native macOS SSH/SFTP file browser and download manager. It connects directly to servers configured and controlled by the user; it does not operate an SSH/SFTP server or require a CloudBridge account.

A dedicated public review server is not available for this initial submission. A complete demonstration video must be attached before submission.

Demo video: ${REVIEW_VIDEO_URL}

The video must show server setup, browsing, Quick Look, file and folder downloads, queue controls, Finder reveal, disconnect, and relaunch/reconnection with the password stored in macOS Keychain.

