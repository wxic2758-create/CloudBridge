# CloudBridge 产品规划

## 意图摘要

将 CloudBridge 发展为一款面向 macOS、首发美国商店并直接提供多语言版本的本地优先远程下载工具：帮助缺乏 SSH 基础知识、无法顺利使用现有 SSH 工具的用户，以清晰、低压力的方式连接自己的 SSH/SFTP 服务器，找到文件、预览文件并可靠地拉回本机。

产品不应演变为云盘、账号系统或团队协作平台。它的差异化来自三点：连接目标由用户掌控、无需开发者后端、远程下载体验比命令行更清晰。

> 当前基础：已有可运行的 macOS MVP，支持密码连接、远程目录浏览、文件/文件夹下载、Quick Look 预览、自定义本地下载目录，以及会话内密码缓存。

## 1. 产品定位

### 一句话定位

**CloudBridge: SFTP Transfer 是一款安静、可信、专注下载的 macOS SFTP 客户端，副标题为“Remote File Download”。**

### 用户问题

- 从服务器拉日志、构建产物、备份或媒体文件时，不想反复记忆 `scp` 命令。
- 现有通用 SSH 客户端功能过重，远程文件浏览和下载不是第一优先级。
- 不希望为了连接自己的服务器注册第三方账号或把服务器信息交给云端。
- 下载失败、目标文件已存在、目录较大时，需要明确的状态和可恢复反馈。

### 产品承诺

- **快**：首次连接路径短，常用服务器可一键重连。
- **稳**：下载过程可见、失败原因可理解、不会静默覆盖本地文件。
- **私密**：无开发者运营后端；凭据只在用户选择的服务器连接中使用。
- **专注**：围绕“找到并带走文件”设计，而不是堆叠终端或云盘功能。

## 2. 目标用户与场景

### 主要用户

| 用户 | 高频任务 | 成功标准 |
| --- | --- | --- |
| SSH 初学者和基础知识较少的用户 | 从自己的服务器取回日志、文档、压缩包或媒体文件 | 不看 SSH 教程、不打开终端，也能完成首次连接和下载 |
| 需要偶尔取文件的 macOS 用户 | 连接一台服务器并找到指定文件 | 能理解每个字段和状态，不因连接错误而卡住 |
| 独立开发者和技术支持人员 | 拉取构建包、日志和诊断文件 | 在简单流程中快速完成下载，并在失败后知道如何恢复 |

### 非目标用户

- 需要多人共享空间、权限管理或审计报表的团队。
- 需要持续双向同步、版本控制或自动化部署的高级 DevOps 用户。
- 只需要一次性网页上传/下载的普通用户。
- 目标是管理服务器、执行命令或配置复杂自动化流程的高级用户。

### 核心场景

1. 输入主机、端口、用户名和凭据，连接到远程目录。
2. 通过路径与列表快速定位目标文件。
3. 预览文件确认内容，再下载文件或整个文件夹。
4. 下载完成后打开本地位置，或继续处理下一台服务器。

## 3. 产品目标与指标

### 北极星指标

**首次使用 CloudBridge 的目标用户成功完成一次远程文件下载的比例。**

产品无开发者后端时不应强行引入追踪。当前规划阶段不以用户招募或外部测试渠道为前置条件；以下指标先作为产品验收和后续验证框架，未来可通过本地可选诊断、用户访谈、崩溃报告（若启用且明确告知）和美国商店反馈获得：

- 首次连接成功率。
- 从启动到首次成功下载的时间。
- 首次使用时无需外部教程或终端辅助即可完成下载的比例。
- 首次连接流程中因术语、字段或错误信息产生的求助点数量。
- 下载失败率及失败后重试成功率。
- 重复使用保存服务器的比例。
- 用户主动反馈的“找文件耗时”和“下载不确定感”。

### 近期目标（0.2）

- 让小白用户可以在明确解释下完成首次连接，并安全地保存和管理多个服务器配置。
- 将密码认证与 SSH 私钥认证都做成可理解、可验证的连接路径，私钥认证列为 P0。
- 让下载成为可观察、可取消、可重试的任务。
- 让远程目录在路径深、文件多、网络慢时仍可用。
- 从首发版本开始支持规划内的多语言市场，所有核心流程使用可翻译资源，不把英文文案硬编码在界面中。

### 中期目标（0.3）

- 在用户明确开启后，通过 macOS Keychain 保存凭据。
- 支持搜索、排序、书签和最近使用，减少重复理解路径的成本。
- 支持下载历史和本地结果定位。

### 长期探索（0.4+）

- 双栏远程/本地浏览。
- 基于书签的常用远程路径。
- 可选的同步或上传能力。

这些长期能力必须建立在下载核心稳定之后，不能提前稀释产品定位。

## 4. 信息架构

### 一级结构

- **服务器**：保存的服务器列表、添加/编辑、连接状态。
- **远程浏览器**：当前服务器、路径、文件列表、搜索和排序。
- **下载任务**：进行中、已完成、失败、取消。
- **设置**：默认下载目录、认证与主机密钥、隐私说明。

### 推荐窗口结构

左侧为服务器与连接区，中央为远程浏览器，底部或独立抽屉为下载任务。当前 MVP 的连接表单 + 文件表格方向可保留，但服务器管理和任务状态不应继续挤在同一侧栏中。

### 关键对象

- `ServerProfile`：名称、主机、端口、用户名、认证方式、默认远程路径。
- 凭据默认不随服务器配置保存；只有用户明确开启凭据保存后，才通过 macOS Keychain 保存。
- `RemoteItem`：名称、类型、路径、大小、修改时间、权限/可读状态。
- `DownloadTask`：来源服务器、来源路径、目标路径、进度、速度、状态、错误。
- `HostIdentity`：主机指纹、首次信任时间、最近验证状态。

## 5. 核心流程

### 流程 A：首次连接

1. 用户点击“添加服务器”。
2. 输入服务器名称、地址、端口、用户名和认证方式。
3. 首次连接时展示主机指纹和信任风险，用户明确确认后继续。
4. 连接成功后进入默认远程路径。
5. 空状态提示下一步：浏览、搜索或直接下载。

### 流程 B：定位并预览

1. 在路径栏查看当前位置，可返回上级或进入书签路径。
2. 使用搜索、排序和类型筛选缩小列表。
3. 单击选择，双击目录进入，双击文件预览。
4. 预览失败时提供“直接下载”与可读错误原因，而不是只显示通用弹窗。

### 流程 C：下载

1. 选择一个或多个文件/文件夹。
2. 点击唯一的主操作“下载”。
3. 若首次使用，选择本地目标目录并记住安全书签。
4. 在任务面板看到进度、速度、剩余时间和当前文件。
5. 完成后提供“在 Finder 中显示”，冲突时让用户选择替换、跳过或重命名。

### 流程 D：异常恢复

- 连接超时：显示主机、端口和超时原因，提供“重试”和“编辑连接”。
- 主机指纹变化：阻止静默连接，要求重新确认或删除旧指纹。
- 权限/沙盒失败：直接引导重新选择目标目录。
- 下载中断：保留失败任务，支持重试；明确是否从头开始或续传。
- 远程路径不存在：回退到上一级，并保留用户当前路径上下文。

## 6. 功能优先级

### P0：发布后必须补齐

- 保存服务器配置中的非敏感信息；0.2 默认不持久化密码和私钥凭据，并明确告知用户保存边界。用户主动开启后的 macOS Keychain 集成列入 0.3。
- SSH 私钥认证、密码短语输入和私钥文件选择，支持首次连接时的逐步引导。
- 主机密钥指纹展示、变更告警和可管理的已信任主机列表。
- 下载任务列表：进度、取消、失败、重试、完成后打开 Finder。
- 单选之外的多选下载与批量文件夹下载。
- 连接表单校验：地址、端口、用户名、密码/密钥缺失与格式错误。
- 关键操作的键盘操作和 VoiceOver 标签。
- 多语言基础设施：界面文案、错误信息、系统通知、无障碍标签和 App Store 元数据均可本地化；首发语言清单另行确认。

### P1：提升效率

- 远程文件名搜索、按名称/大小/修改时间排序。
- 常用远程路径书签。
- 最近连接和最近下载记录，仅存储在本机。
- 用户主动开启后，通过 macOS Keychain 保存密码或私钥相关凭据。
- 下载冲突策略与按任务查看错误详情。

### P2：形成专业壁垒

- 远程/本地双栏视图。
- 下载队列并发数设置和带宽限制。
- 断点续传与校验摘要。
- 多窗口、多服务器并行浏览。

### 明确暂不做

- 开发者运营的账户、云端同步和文件托管。
- 团队共享、邀请、权限角色和在线协作。
- 内置终端，避免与成熟 SSH 终端直接竞争。
- 上传、删除、重命名等远程高风险写操作，当前暂不接受，避免扩大新手用户的误操作风险。
- 没有明确用户需求支撑的 AI 搜索或自动整理。

## 7. 交互与视觉原则

- 延续 macOS 原生、轻量、工具感强的视觉语言，不做云盘式营销界面。
- 连接状态、当前服务器和当前路径始终可见。
- 下载是全局唯一主操作；“连接”“预览”“打开 Finder”使用层级更低的语义按钮。
- 状态反馈优先使用任务栏/内联状态，弹窗只处理需要用户决策的错误。
- 密码、主机指纹和沙盒权限属于高信任信息，文案应具体说明“存在哪里、发给谁、何时失效”。
- 列表密度服务于扫描：名称优先，大小和修改时间对齐，超长路径中间截断。
- 空状态不只是“没有内容”，要给出下一步动作和原因。
- 多语言文案优先保证清晰和可翻译：避免依赖英文长度、双关语或难以直译的技术缩写；布局必须为不同语言长度预留空间。

## 8. 技术与安全约束

- 继续优先使用 macOS 原生能力和系统 SSH/SFTP 工具，减少第三方依赖面。
- 将传输层抽象为可替换接口，为未来原生 SFTP 库和更可靠的进度回调留出空间。
- 密码和私钥凭据默认不落盘；只有用户明确开启后，才允许通过 macOS Keychain 保存。服务器配置始终与认证凭据分离。
- 不默认接受主机密钥变化；为首次信任、变更和撤销提供可解释 UI。
- 所有下载写入都经过安全作用域目录授权，避免把权限失败留给模糊错误。
- 下载任务应与浏览状态解耦，切换路径或服务器不能中断已有任务。
- 对路径、文件名、符号链接、特殊字符和超大目录做测试，避免命令注入和路径混淆。

## 9. 版本路线

### 0.2：可靠下载

交付面向小白用户的服务器保存、密码和私钥认证、任务面板、取消/重试、主机指纹确认、多选下载、完整错误状态和首发多语言支持。发布门槛是：目标用户不看外部文档、不打开终端，也能在任一首发语言下完成“连接 → 定位 → 预览 → 下载 → 在 Finder 中查看”的闭环，且所有失败场景有恢复路径。

### 0.3：高频效率

交付搜索、排序、书签、最近使用，以及用户主动开启后的 Keychain 集成。重点不是功能数量，而是让已有用户少填表、少记路径、少等待。

### 0.4：专业工作台

用真实用户反馈决定是否进入双栏、本地文件管理、队列并发、断点续传。若下载核心仍有稳定性问题，继续投入可靠性而不是扩展边界。

## 10. 验收检查

### 产品验收

- 小白用户不看外部文档、不打开终端即可完成首次连接和一次下载。
- 连接表单中的主机、端口、用户名、密码、私钥等术语都有就地解释，用户能理解为什么需要填写。
- 用户能明确区分未连接、连接中、已连接、下载中、完成和失败。
- 服务器配置、下载目标、主机信任状态和 Keychain 凭据的持久化边界清楚；凭据保存必须由用户主动开启。
- 首发语言中的核心流程、错误恢复、无障碍标签和系统提示均已翻译，且不存在截断、溢出、混用语言或依赖英文语序才能理解的文案。
- 任一失败任务都能重试、编辑配置或回到可用状态。
- 不会静默覆盖本地同名文件。

### 工程验收

- 覆盖密码连接、私钥连接、密码短语、主机密钥首次确认和变更场景。
- 覆盖单文件、文件夹、多选、特殊字符、中文文件名和大文件下载。
- 覆盖网络断开、超时、远程权限不足、本地沙盒权限失效。
- Debug/Release 构建、签名、沙盒和干净用户环境均通过。
- 隐私政策、App Store 描述和实际行为一致。

## 11. 已确认决策

- [x] 首个市场：仅面向美国商店；当前产品平台为 macOS。
- [x] 服务器保存策略：默认只保存非敏感信息；用户明确开启后，才通过 macOS Keychain 保存凭据。
- [x] 0.2 认证范围：将 SSH 私钥认证列为 P0，与密码认证一起交付。
- [x] 产品名称副标题：远程下载管理。
- [x] 高风险写操作：暂不接受上传、删除、重命名等远程写操作。
- [x] 首要用户：小白用户或 SSH 基础知识缺乏、无法顺利使用现有 SSH 工具下载文件的用户。
- [x] 首要验证假设：目标用户无需外部教程或终端辅助，也能顺畅完成首次连接、找到文件并下载。
- [x] 首发语言策略：首发版本直接提供多语言国家/地区版本，不以单一英文版本作为首发前置；具体语言清单与商店地区配置另行确认。

## 12. 待补充事项

- [ ] 0.2 的“顺畅完成”具体门槛，例如首次任务完成率、完成时长和允许的求助次数；在没有外部招募渠道前，先作为内部验收标准。
- [ ] 首发语言清单、各语言对应的美国商店本地化元数据，以及术语审核方式。
- [ ] 后续获得测试渠道后，再补充样本数量、招募渠道和访谈方式；这不是 0.2 产品规划或开发的前置条件。

## 下一步

本规划已足以指导产品设计和 0.2 开发，不需要等待用户招募渠道。后续以本文件作为下一轮设计和实现的唯一输入，先生成 0.2 的交互原型，再拆解为新手引导与连接、服务器管理、远程浏览器、下载任务和多语言基础设施五个可独立验收的模块。获得真实测试渠道后，再补充量化门槛和验证样本，不改变当前核心产品边界。
